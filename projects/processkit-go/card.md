---
repo: processkit-go
type: card
kind: library
language: Go (bindings/FFI)
status: no-repo-yet
publishes: ["Go module (план)"]
depends-on: [ProcessKit-rs]
depended-on-by: []
pair: null
updated: 2026-06-09
---

## Назначение
**(Репозитория ещё нет — планируется.)** Go-биндинги/обёртка к ядру `processkit` (Rust).
Как и для `processkit-py`, ядро — единственный источник истины; Go-слой тонкий. Детали — в
мигрированном roadmap.

## Ответственность / границы
**Входит (будет):** идиоматичная Go-поверхность над FFI ядра.
**НЕ входит:** логика ядра → `ProcessKit-rs`.

## Публичный интерфейс / точки входа
Уточняется в roadmap (FFI-граница, модель teardown под Go).

## Сборка и тесты
```
(когда заведётся репо)
```

## Связи и зависимости
Биндинг к `ProcessKit-rs`; следует за версией ядра осознанно.

## На что смотреть при кросс-репо изменениях
Та же асимметрия teardown, что и у py: разобрать в roadmap. Решение «биндинг vs реимплементация»
зафиксировано в пользу тонкого биндинга (см. roadmap).

## Ссылки
- Roadmap (мигрирован): `./ROADMAP.md`
- Источник истины: `../ProcessKit-rs/card.md`
