# Прогон `fixture-20260609` — валидация P1 (Дирижёр `/comms`)

Валидационный прогон Дирижёра на **синтетической** фикстуре
`../../_fixtures/sample-inbound/` (НЕ реальное входящее). Реальные `comms`/`tasks/QUEUE.md` не
изменялись; тред `T-20260609-vcs-processkit-feedback` пропущен.

Поток: `hq-triage` (субагент) → `accept` → `hq-planner` (субагент) → 6 подзадач + граф + волны.

Артефакты:
- `tick.json` — журнал прогона.
- `triage/T-fixture-sample.json` — решение триажа (accept + seed).
- `proposed-replies/T-fixture-sample.md` — черновик ответа в тред.
- `proposed-queue-rows.md` — предлагаемые строки очереди + волны (граф зависимостей).
- `proposed-tasks/TASK-0005-*.md` — пример спеки задачи (полный набор 0005–0010 пишется Дирижёром при `--apply`).

Вывод: оба специалиста (triage, planner) дали корректный структурный результат по схемам;
граф зависимостей верный (0006 после 0005; 0007 после 0005+0006; 0008/0009 параллельно; 0010 — другой репо).
